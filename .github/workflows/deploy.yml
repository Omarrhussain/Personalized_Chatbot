name: Deploy Chatbot

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Test imports
      run: |
        python -c "from src.model.gemini_rag_system import GeminiRAGSystem; print('✅ Import test passed')"
        python -c "import streamlit; print('✅ Streamlit test passed')"
        python -c "import google.generativeai; print('✅ Gemini test passed')"

  deploy-notification:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deployment Ready
      run: |
        echo "🚀 Code is ready for manual deployment!"
        echo "1. Deploy FastAPI to Railway: https://railway.app"
        echo "2. Deploy Streamlit to Streamlit Cloud: https://share.streamlit.io"
        echo "✅ CI/CD tests passed - ready for deployment"