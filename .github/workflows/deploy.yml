name: Test Chatbot

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run basic tests
      run: |
        python -c "print('✅ Testing imports...')"
        python -c "import streamlit; print('✅ Streamlit works')"
        python -c "import google.generativeai; print('✅ Gemini works')"
        python -c "from langchain_community.vectorstores import FAISS; print('✅ LangChain works')"
        python -c "print('🎉 All tests passed! Ready for deployment.')"